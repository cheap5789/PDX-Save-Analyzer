# === Python ===
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# === Node / Frontend ===
node_modules/
frontend/dist/
frontend/.vite/
.npm/

# === Database & runtime data ===
data/*.db
data/*.db-shm
data/*.db-wal

# === User config (runtime, not committed) ===
data/user_config.json

# === Save files (never commit game saves) ===
*.eu5
*.eu4
*.ck3
*.hoi4
*.v3

# === OS / Editor ===
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo

# === Logs ===
*.log
logs/

# === Secrets / env ===
.env
.env.*
!.env.example

# === Proprietary game data (never publish) ===
# EU5 base game config and localisation files
# Drop these into game-data/ locally for toolbox use — never committed
# The README.md inside game-data/ IS committed (it's just instructions)
game-data/**
!game-data/README.md
